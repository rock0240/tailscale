# network-e2e-tests

End-to-end integration tests for network connectivity and mesh routing.

## Setup

```bash
npm install
```

## Running Tests

```bash
npm test
```

## CI

Tests run automatically via GitHub Actions on every push to `main`.
